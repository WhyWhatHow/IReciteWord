<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sdut.soft.ireciteword">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/dictlogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/dictlogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".ICiBaActivity"></activity>
        <activity android:name=".MainActivity">
             <intent-filter>
             <action android:name="android.intent.action.MAIN" />
             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity> <!-- test for toolbar  && search service -->
        <activity android:name=".activity.toolbar.ToolDemoActivity">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->
        </activity>
        <activity android:name=".MenuActivity" />
        <activity android:name=".ReviewActivity" />
        <activity
            android:name=".DetailActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".BaseSettingActivity" />
        <activity android:name=".PwdActivity" />
        <activity android:name=".WordSpecificActivity" /> <!-- TODO add BaseActitvity to fix  the screen -->
        <activity
            android:name=".activity.toolbar.ToolBarActivity"
            android:label="@string/tool_bar" />
        <activity
            android:name=".activity.toolbar.ZhiHuActivity"
            android:label="@string/zhi_hu"
            android:theme="@style/Theme.ZhiHu" />
        <activity
            android:name=".AboutActivity"
            android:theme="@style/Theme.Other" />
    </application>

</manifest>